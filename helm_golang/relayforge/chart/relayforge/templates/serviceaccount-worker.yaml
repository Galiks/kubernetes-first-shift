apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "relayforge.fullname" . }}-worker
  labels:
    {{- include "relayforge.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
automountServiceAccountToken: false