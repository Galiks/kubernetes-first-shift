apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: pavel-lab
spec:
  type: ClusterIP
  selector: 
    app: web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP