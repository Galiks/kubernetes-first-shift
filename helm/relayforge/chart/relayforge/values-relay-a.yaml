clientAuthSecret:
  name: relay-a-client-auth
  key: client-token

worker:
  signingSecret:
    name: relay-a-signing
    key: signing-key

testSink:
  verificationSecret:
    name: relay-a-verification
    key: verification-key
  controlSecret:
    name: relay-a-verification
    key: control-token

destinations:
  test:
    url: "http://{{ .Release.Name }}-relayforge-test-sink:8080"